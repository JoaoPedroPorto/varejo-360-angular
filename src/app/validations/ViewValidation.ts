export class ViewValidation {

    public isValidMultipleSelect(array: Array<string>): boolean {
        if (array.length !== 0) {
            return true;
        }
        return false;
    }

// tslint:disable-next-line:eofline
}